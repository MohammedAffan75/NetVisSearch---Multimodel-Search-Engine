# docker-compose.yml
# Placeholder — will be filled out once the frontend scaffold is ready.
# Intended services:
#   backend  — FastAPI + Uvicorn
#   frontend — React + Vite dev server
#
# Example structure (uncomment and fill in when ready):
#
# version: "3.9"
#
# services:
#   backend:
#     build:
#       context: .
#       dockerfile: docker/backend.Dockerfile
#     ports:
#       - "${APP_PORT:-8000}:8000"
#     env_file: .env
#     volumes:
#       - ./data:/app/data
#       - ./model_cache:/app/model_cache
#
#   frontend:
#     build:
#       context: .
#       dockerfile: docker/frontend.Dockerfile
#     ports:
#       - "5173:5173"
#     depends_on:
#       - backend
